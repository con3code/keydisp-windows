# Windows 実機テスト チェックリスト

macOS/CI では検証できない項目の蓄積。**上から優先順** (フック基盤が最優先 — ここが崩れると全部やり直しになる)。
各フェーズの実装時に追記していくこと。

検証記録: ✅VM = Parallels 仮想環境で確認 (2026-08-18、MVP 8e53d74 / Phase3 b2d12a2)。実機での再確認は任意。

Phase 3 (b2d12a2) の VM 確認結果:
- 修飾キーの押下即時表示 OK (単独修飾キーが押している間に出る)
- 3 スタイル (シンプル/キーキャップ/カスタム画像) の切り替え・描画 OK
- アニメーション OK (spring・×n パルス・フェードアウトの滑らかさ)
- 折り返しも問題なし

## A. フック基盤 (Phase 2) — 最優先

- [x] ✅VM WH_KEYBOARD_LL でキーが取れる (通常キー / 修飾キー。メディアキーは未確認)
- [x] ✅VM 押しっぱなしリピートの自前合成が正しい (×n カウント動作)
- [x] ✅VM 高速タイピングで取りこぼし・行化けがない
- [ ] フックタイムアウト → watchdog による再インストールが機能する (重負荷時)
- [ ] スリープ復帰・セッションロック解除後もキーが取れる
- [ ] 管理者昇格アプリにフォーカス中はキーが取れない (UIPI) — 想定どおりの挙動として確認
- [ ] UAC ダイアログ (Secure Desktop) 中に状態が壊れない (reconcile で回復)
- [ ] トレイアイコン表示・メニュー・explorer.exe 再起動後の再登録 (TaskbarCreated)
- [ ] RegisterHotKey が発火する / 他アプリと衝突しない既定を確認 (既定 Alt+Win+K = MOD_ALT|MOD_WIN + 0x4B)
- [ ] 単一インスタンス Mutex (2 つ目の起動が即終了する)
- [ ] トレイメニュー: キー表示トグル / すべてのキー入力を表示 / 設定フォルダを開く / 終了
- [ ] IME オン状態での ToUnicodeEx (文字ラベルが化けないか) と ImmGetOpenStatus 判定の初動確認

## B. オーバーレイ (Phase 2-3)

- [x] ✅VM 透明・クリック透過 (下のアプリを普通に操作できる)
- [ ] TOPMOST が維持される (他の topmost アプリ、全画面動画、PowerPoint スライドショー上)
- [ ] 排他フルスクリーンのゲーム上での挙動確認 (表示されない場合は既知の制約として文書化)
- [ ] 行なし時に Hide → 新規行で再表示・TOPMOST 再主張
- [ ] 描画負荷: 4 行×長いコンボ連打で CPU/GPU が許容範囲か
- [ ] 見た目係数の調整 (OverlayConstants.cs): rowHeight / keycap 寸法 / フォント

## C. 編集モード・マルチモニタ・DPI (Phase 4)

Phase 4 (98875c1) の VM 確認: 破線枠+HUD 表示・移動/リサイズ・中心スナップ・完了動作・位置永続化 OK。
発見された不具合 (修正済み → 再確認要):
- オーバーレイが HUD の前面に来て操作しづらい → 編集モード中は TOPMOST 再主張を停止
- シンプル/カスタム画像の中央・右揃えで文字ズレ → OutlinedTextBlock の原点補正
- 編集モード終了後、キー表示再開時に編集画面が 1 フレーム見える → Hide を 1 フレーム遅延

- [x] ✅VM 編集モード: トレイから入って破線枠+サンプル行が出る / HUD が開く
- [x] ✅VM 編集モード: 内側ドラッグで移動、端 8px ドラッグでリサイズ (SC_SIZE 方式)
- [ ] 編集モード: ドラッグ中のフェード凍結 (WM_ENTERSIZEMOVE/EXITSIZEMOVE)
- [x] ✅VM 中心スナップ (10px) とアンバー破線ガイドの表示
- [x] ✅VM HUD の各コントロールが動作・「完了」で終了
- [ ] HUD より前面に破線枠が来ない (修正後の再確認)
- [ ] 中央揃え・右揃えの文字位置 (修正後の再確認、シンプル/カスタム画像)
- [ ] 編集モード終了後のフラッシュが出ない (修正後の再確認)
- [ ] 「この画面ではキー表示を出さない」→ 完了後にその画面で非表示
- [x] ✅VM 位置とサイズが再起動後も復元される (overlayFrame)
- [ ] 表示位置をリセットで既定位置+記憶クリア
- [ ] DPI 混在 (100%+150% 等) でオーバーレイの位置・サイズが正しい
- [ ] モニタ抜き差しでプロファイルが維持される (monitorDevicePath の安定性)
- [ ] カーソル画面追従 (followCursorScreen=true)・画面またぎドラッグでのプロファイル切替
- [ ] displayScale/maxRows 増加で表示領域が自動拡張される (growToFitContent)

## D. IME・日本語 (Phase 5)

VM 検証 (2026-08-18): kanaDisplay は表示に至らず (IME オン判定が機能していない疑い)。
かな・IME 切替キーの表記は Mac との割り当て差により再設計が必要 → SPEC.md §9.5 に課題を集約。

- [ ] ImmGetOpenStatus による日本語入力モード判定 (MS-IME / Google 日本語入力 / ATOK)
- [ ] JIS かな配列表示の全キー確認 (゛゜「」の位置)
- [ ] 半角全角・無変換・変換・カタカナひらがなキーの表示
- [ ] ToUnicodeEx がキーボード状態を壊さない (フラグ 0x4、デッドキーのあるレイアウトでも)

## E. 周辺機能 (Phase 5)

- [ ] マウスハイライトの追従・左右の見た目差 (左=塗り+リング / 右=二重リング)・離した後 0.25s フェード
- [ ] 巨大ポインタの追従・サイズ/色変更・先端位置がカーソルと一致
- [ ] ホットエッジ: 上端でフェード凍結 / 下端で一時非表示。下端とタスクバー(自動非表示含む)の競合確認
- [ ] dragToMove: 行の上でだけ掴める (行の外は下のアプリへ素通し)・掴んで移動・移動後の位置保存
- [ ] 設定画面: 5 タブの各項目が反映される・言語切替で文言が変わる
- [ ] ホットキーレコーダ: クリック→キー入力で変更・Shift 単独拒否・Esc キャンセル・変更後に即再登録
- [ ] 初回起動時のプライバシーダイアログが 1 回だけ出る
- [ ] 自動起動 (HKCU Run) の登録・解除・再ログイン後の起動
- [ ] 設定 json の保存・復元・破損時のフォールバック
- [ ] HUD の z-order: 破線枠をドラッグした後も HUD が前面に残る (Owner 方式の再確認)

## F. 配布 (Phase 6)

- [ ] zip 展開 → 実行 (SmartScreen の挙動確認、「詳細情報」→実行)
- [ ] self-contained 単一 exe が .NET 未導入マシンで動く
- [ ] Windows Defender 誤検知の有無 (低レベルフックを使うため要確認)
- [ ] x64 実機 + (可能なら) ARM64 Windows での動作
